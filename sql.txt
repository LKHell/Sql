1/
SELECT *
FROM Student
WHERE Department='IS' OR Department='CS'
ORDER BY Department ,StudentID;

2/
SELECT *
FROM Student
WHERE age=(SELECT MIN(age) FROM Student)

3//////¸ÄÒªÇó name like English
SELECT Coursename 
FROM Course
WHERE Coursename Like '%English%'

4/
SELECT CourseID,CourseName,Credit
FROM Course
WHERE CourseBefore IS NULL ;

5/ LiA the score of C
SELECT Student.StudentID,StudentName,Score
	FROM Student,Choose,Course
Where Student.StudentID=Choose.StudentID AND Choose.CourseID=Course.CourseID
AND StudentName='LiA'AND CourseName = 'C';

6/
select StudentID 
	from Choose 
	Group by StudentID 
	having AVG(Score)>80; 

7/
SELECT StudentName
FROM Student
Where StudentID not in
		(
			select  StudentID
			FROM Choose,Course
			WHERE Choose.CourseID=Course.CourseID
			AND CourseName ='Database'
		)
		AND department = 'CS';

8/
